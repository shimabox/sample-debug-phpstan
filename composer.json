{
    "require": {
        "nesbot/carbon": "^2.72"
    },
    "require-dev": {
        "phpstan/phpstan": "^1.10"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "phpstan": "phpstan analyse",
        "phpstan:6": "phpstan analyse -l 6 -c phpstan.level6.neon"
    }
}
